[package]
name = "ouroboros-rex"
version = "0.1.0"
edition = "2024"

[dependencies]
ouroboros = { path = "../ouroboros", default-features = false, features = ["serde"] }
ouroboros-proc-macro = { path = "../ouroboros-proc-macro" }
rex = { path = "../rex" }
